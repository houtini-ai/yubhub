{
  "name": "yubhub-mcp",
  "version": "2.2.0",
  "description": "Yubhub MCP server for job feed management with dashboard UI",
  "type": "module",
  "bin": {
    "yubhub-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@mcp-ui/server": "^5.16.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
